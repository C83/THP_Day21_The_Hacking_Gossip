<%= form_for @c do |f| %>
	<div class="form-group">
		<label> Author : </label>
		<%= f.text_field :anonymous_commentor %>
	</div>
	<div class="form-group">
		<label> Content : </label>
		<%= f.text_field :body %>
	</div>
        <%= hidden_field_tag 'gossip_id', @g.id  %>
   	<div class="form-group">
		<button type:"submit" class="btn btn-primary"> Submit </label>
	</div>
<% end %>